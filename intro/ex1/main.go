package main

import "fmt"

func main() {
	fmt.Println("Hello, world!")
}

// go run main.go
// package (a way to group function), imports (useful external functions) and a main function
// go manages all dependencies in its module, that's why we need to use go mod init command
